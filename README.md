# agent-tree-lab

A deliberately small, deliberately broken Python project.

It exists so a tree of subagents has something honest to work on.

Checks:
- ruff check .
- mypy app
- python -m compileall -q app

Tests:
- pytest -q
